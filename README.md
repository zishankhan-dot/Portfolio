# Portfolio- wep page build on react ..
have individual components inside /src/components - each component work on individual task(create cards by looping through arrays)
the data used is static can be found : /src/data/ 
components are then being used in App.js ( starting point -> index.js)
uses ES-module 
